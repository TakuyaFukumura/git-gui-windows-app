# Git GUI Windows App リファクタリング残作業計画書

## 1. 目的

本書は、2026-09-09時点で未完了のリファクタリング作業だけを記録する。完了済みの基盤整備や状態管理の作業は対象外とし、残作業の実施順序、完了条件、検証方法を定める。

リファクタリング中も、ユーザーが認識できる動作、Gitコマンドの安全な引数分離、既存設定ファイルとの互換性を維持する。機能追加やUIデザインの刷新は本書の対象外とする。

## 2. 残作業に共通する制約

- `GitGuiWindowsApp`はJavaFXのライフサイクルとアプリケーションの配線に限定する。
- UI以外のクラスからJavaFXコントロールを参照しない。
- `model`と`application`はJavaFX型に依存しない。
- Gitコマンドはシェルを介さず、引数リストで実行する。
- パス引数の前には`--`を置き、既存のエラー処理と割り込み処理を維持する。
- `%USERPROFILE%\.git-gui-windows-app\settings.properties`の既存キー、形式、保存場所を変更しない。
- 各作業の終了時に、関連テストと`mvnw.cmd clean install`または`mvn clean install`を実行する。
- 構造変更と機能変更を同じコミットに混在させない。

## 3. 未完了作業

### Step 1: パッケージ間の依存方向を文書化・固定する

#### 残っている作業

- `application`層、`ui`層、`git`層、`config`層、`model`層の依存方向を文書化する。
- `GitGuiWindowsApp`から各サービスを直接組み立てている箇所を確認し、画面がGit実装の詳細へ依存しない境界を定める。
- `model`、`application`、`config`、`git`にJavaFX依存がないことを確認する。
- パッケージ間の循環依存と、UIから下位層への不要な参照を解消する。

#### 完了条件

- パッケージの依存方向と各層の責務が文書化されている。
- UI以外のクラスからJavaFXクラスへの参照がない。
- パッケージ整理後も機能とテスト結果が変わらない。

#### 検証

- `mvnw.cmd clean test`
- パッケージごとの`import`を確認する。

### Step 4: 画面を機能単位のView/Controllerへ分割する

#### 目的

画面構築と機能ごとのイベント処理を分離し、`GitGuiWindowsApp`の変更範囲を小さくする。

#### 作業内容

- リポジトリ・ブランチ操作を`RepositoryToolbar`相当のクラスへ移す。
- 変更一覧と差分表示を`ChangesPane`へ移す。
- ステージ・アンステージ・コミットを`CommitActionBar`へ移す。
- 履歴検索、履歴一覧、詳細表示を`HistoryPane`へ移す。
- 確認・エラー・共通スタイルのダイアログ生成を`DialogService`へ移す。
- 各Viewは`GitService`や個別のGit実装を直接呼ばず、アプリケーション層の操作を呼び出す。
- FXMLやDIフレームワークは導入せず、既存のJavaFXコードによる構築を維持する。

#### 完了条件

- `GitGuiWindowsApp`がStage、Scene、全体レイアウトの組み立て、ライフサイクル管理、依存関係の配線に限定される。
- 機能別クラスが互いのJavaFXコントロールを直接操作しない。
- 最小ウィンドウサイズ、テーマ、タブ構成、表示文言、ボタン有効条件を維持する。

#### 検証

- 起動、リポジトリ選択、変更表示、差分表示、ステージ、コミット、ブランチ操作、履歴表示をGUIで確認する。
- `mvnw.cmd clean install`

### Step 5: `GitService`内部の実装を用途別Facadeへ移行する

#### 残っている作業

- `RepositoryService`へリポジトリ検証、status、HEAD取得の実装を移す。
- `ChangeService`へdiff、stage、unstageの実装を移す。
- `BranchService`へ一覧、作成、切替、削除、ブランチ名検証の実装を移す。
- `HistoryService`へコミット履歴と参照情報の取得を移す。
- `CommitService`へコミット実行とコミットID取得を移す。
- 共通の`GitCommandRunner`、例外、結果モデル、Parserを再利用する。
- 移行完了後に旧`GitService`の呼び出し、不要な委譲、重複実装を削除する。

#### 完了条件

- 各Facadeが担当領域外のGitコマンドを実行しない。
- UIから旧`GitService`の実装詳細へ到達できない。
- Git引数の分離、`--`によるパス境界、失敗時の例外処理を維持する。
- 旧`GitService`が不要な互換Facadeとして残っていない。

#### 検証

- 各Facadeのサービス単体テストを追加・更新する。
- 一時Gitリポジトリでstatus、diff、stage、unstage、commit、branch、historyを確認する。
- `mvnw.cmd clean install`

### Step 6: Parserとモデルの入力契約を強化する

#### 残っている作業

- `GitStatusParser`、`GitDiffParser`、`GitBranchParser`、`GitCommitParser`の入力と出力契約を明文化する。
- 区切り文字、空入力、文字コード、特殊文字、異常形式に対する扱いを統一する。
- 不正なGit出力を黙って成功扱いしないエラー境界を定める。
- `StageState`、変更種別、参照種別などの状態の組み合わせを整理する。
- 不変モデルのnull許容範囲と空コレクションの方針を明文化する。
- UI表示用の文字列生成がParserへ混在していないことを確認する。

#### 完了条件

- ParserがJavaFXに依存せず、入力文字列から明確なモデルを返す。
- 正常系、空入力、異常形式で結果とエラーの扱いが一貫している。
- UI用の表示文言がParserやモデルに混在していない。

#### 検証

- 正常、空、特殊文字、バイナリ、巨大差分、複数親コミットのテストを追加・更新する。
- `mvnw.cmd clean test`

### Step 7: 設定の既定値とカテゴリを分離する

#### 残っている作業

- `ApplicationSettings`に集中している既定値を一箇所へ集約する。
- ウィンドウ状態、Git実行ファイル、最近のリポジトリ、基準フォルダーをカテゴリ別に扱う。
- `SettingsRepository`を永続化の入口として整理し、設定値の検証を設定モデルまたは専用の検証処理へ分離する。
- `ThemeService`を設定の読み書きから独立したJavaFXテーマ適用境界として維持する。
- 壊れた設定値、保存失敗、親ディレクトリ作成失敗の扱いを明文化し、既存挙動を固定する。

#### 完了条件

- 既定値が重複せず、一箇所から確認できる。
- 設定カテゴリごとの責務と保存キーの対応が明確になっている。
- 設定の読み書きをJavaFXなしでテストできる。
- 既存の設定ファイルを読み込め、起動時復元と終了時保存の挙動が変わらない。

#### 検証

- `ApplicationSettingsTest`を更新して実行する。
- 一時ファイルで既定値、破損値、重複した最近のリポジトリ、保存失敗を確認する。

### Step 8: テスト構成と移行後のコードを整理する

#### 残っている作業

- テストを`config`、`git`、`application`、`model`の責務に対応するパッケージへ整理する。
- 一時Gitリポジトリ生成の共通Fixtureを作成する。
- Git実行を差し替え可能なテスト用境界を整備する。
- UIから分離できる判断ロジックをヘッドレステストへ移す。
- READMEのプロジェクト構成と本書の完了状況を更新する。
- 未使用クラス、旧Facade、重複ヘルパー、移行用コードを削除する。

#### 完了条件

- 構造変更後の全テストが成功する。
- 主要なGit操作、Parser、設定処理、状態判断に回帰テストがある。
- `GitGuiWindowsApp`がアプリケーションの配線とJavaFXライフサイクルに集中している。
- 未使用の旧クラスや移行用コードが残っていない。

#### 検証

```cmd
mvnw.cmd clean install
```

GUI環境では、リポジトリ選択、変更・差分、ステージ、コミット、ブランチ操作、履歴検索、テーマ切替、終了時保存を一通り手動確認する。

## 4. 実施順序と完了判定

Step 1を先に完了し、Step 4とStep 5を並行して進める。Step 6とStep 7は各実装移行の影響を受けるため、変更対象を確定した後に完了させる。Step 8を最後に実施する。

以下をすべて満たした時点で本計画を完了とする。

- Step 1、4、5、6、7、8の完了条件を満たしている。
- `mvnw.cmd clean install`または`mvn clean install`が成功する。
- 現行機能の手動確認で、リファクタリング前後の挙動差がない。
- Gitコマンドの安全な引数分離と既存設定ファイルの互換性が維持されている。
- READMEとソースのパッケージ構成が一致している。
- 未使用の移行コード、重複処理、不要な互換Facadeが削除されている。

## 5. 残作業のリスクと対策

| リスク | 対策 |
|---|---|
| UI分割時にイベントの接続が外れる | 機能単位に移動し、各移動直後に起動と操作を確認する |
| Facade移行でGit引数やエラー処理の意味が変わる | 既存テストを維持し、コマンド配列と失敗経路を比較する |
| Parserの契約変更で特殊なGit出力を失う | 正常、空、特殊文字、異常形式の入力をテストデータとして固定する |
| 設定カテゴリ分割で既存キーを読めなくなる | キーと保存先を変更せず、旧形式の読み込みテストを追加する |
| 残作業が機能追加に膨張する | 各Stepの対象外を守り、仕様変更は別の計画・コミットに分ける |
